#include "libft.h"

size_t  ft_strlcpy(char * restrict dst, const char * restrict src, size_t dstsize)
{
    size_t i;
    int ind;

    ind = 0;
    while (dtsize && src[ind])
    {
        dst[ind] = src[ind];
        ind++;
        dtsize--;
    }
    dst[ind] = '\0';
    return ()
}